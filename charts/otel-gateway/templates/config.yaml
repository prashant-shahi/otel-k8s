{{- if .Values.config.create }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "otel-gateway.fullname" . }}
  labels:
    {{- include "otel-gateway.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- omit .Values.config "create" | toYaml | nindent 4 }}
{{- end }}